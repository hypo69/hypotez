# Модуль main.py для Telegram-бота цифрового рынка

## Обзор

Этот модуль является точкой входа для Telegram-бота цифрового рынка. Он отвечает за настройку и запуск веб-приложения, обработку входящих запросов через вебхуки, регистрацию промежуточного программного обеспечения (middleware) и маршрутизаторов, а также управление жизненным циклом бота (запуск и остановка).

## Подробнее

Модуль `main.py` выполняет следующие основные функции:

-   Настройка и запуск веб-приложения `aiohttp` для обработки входящих запросов от Telegram через вебхуки.
-   Регистрация промежуточного программного обеспечения для управления сессиями базы данных.
-   Регистрация маршрутизаторов для обработки различных типов запросов, связанных с каталогом товаров, пользовательскими командами и административными функциями.
-   Установка команд по умолчанию для бота, отображаемых в интерфейсе Telegram.
-   Обработка событий запуска и остановки бота, включая отправку уведомлений администраторам и закрытие сессии бота.

## Классы

В данном модуле нет классов.

## Функции

### `set_default_commands`

```python
async def set_default_commands():
    """
    Устанавливает команды по умолчанию для бота.
    """
```

**Назначение**: Устанавливает команды по умолчанию для Telegram-бота. Эти команды отображаются в интерфейсе Telegram и позволяют пользователям взаимодействовать с ботом.

**Параметры**: Нет.

**Возвращает**: Нет.

**Как работает функция**:
-   Определяет список команд, которые будут установлены для бота. В данном случае, это команда `/start` с описанием "Запустить бота".
-   Использует метод `set_my_commands` объекта `bot` для установки команд. Этот метод принимает список команд и область их действия (в данном случае, `BotCommandScopeDefault`, что означает, что команды будут доступны всем пользователям).

**Примеры**:

```python
# Пример вызова функции
await set_default_commands()
```

### `on_startup`

```python
async def on_startup(app):
    """
    Выполняется при запуске приложения.
    """
```

**Назначение**: Выполняет действия при запуске приложения, включая установку команд по умолчанию, настройку вебхука и отправку уведомлений администраторам.

**Параметры**:

-   `app`: Объект приложения `aiohttp`.

**Возвращает**: Нет.

**Как работает функция**:

1.  Вызывает функцию `set_default_commands` для установки команд по умолчанию для бота.
2.  Устанавливает вебхук для бота, используя URL, полученный из настроек (`settings.get_webhook_url`). Это позволяет Telegram отправлять обновления боту через HTTP-запросы.
3.  Отправляет уведомления администраторам о запуске бота. Функция перебирает список идентификаторов администраторов (`admins`) и пытается отправить каждому сообщение "Бот запущен 🥳". Если отправка сообщения не удалась, регистрируется ошибка с использованием `logger.error`.
4.  Логирует информацию об успешном запуске бота с использованием `logger.info`.

**Примеры**:

```python
# Пример вызова функции (обычно вызывается автоматически при запуске приложения aiohttp)
await on_startup(app)
```

### `on_shutdown`

```python
async def on_shutdown(app):
    """
    Выполняется при остановке приложения.
    """
```

**Назначение**: Выполняет действия при остановке приложения, включая отправку уведомлений администраторам, удаление вебхука и закрытие сессии бота.

**Параметры**:

-   `app`: Объект приложения `aiohttp`.

**Возвращает**: Нет.

**Как работает функция**:

1.  Отправляет уведомления администраторам об остановке бота. Функция перебирает список идентификаторов администраторов (`admins`) и пытается отправить каждому сообщение "Бот остановлен. Почему? 😔". Если отправка сообщения не удалась, регистрируется ошибка с использованием `logger.error`.
2.  Удаляет вебхук бота, используя метод `delete_webhook` объекта `bot`. Параметр `drop_pending_updates=True` указывает, что все ожидающие обновления должны быть отброшены.
3.  Закрывает сессию бота, используя метод `close` объекта `bot.session`.
4.  Логирует информацию об остановке бота с использованием `logger.error`.

**Примеры**:

```python
# Пример вызова функции (обычно вызывается автоматически при остановке приложения aiohttp)
await on_shutdown(app)
```

### `register_middlewares`

```python
def register_middlewares():
    """
    Регистрирует мидлвари для диспетчера.
    """
```

**Назначение**: Регистрирует промежуточное программное обеспечение (middleware) для диспетчера (`dp`). Middleware позволяет перехватывать и обрабатывать входящие обновления до того, как они будут обработаны маршрутизаторами.

**Параметры**: Нет.

**Возвращает**: Нет.

**Как работает функция**:

-   Регистрирует два middleware:
    -   `DatabaseMiddlewareWithoutCommit()`: Middleware для управления сессиями базы данных без фиксации изменений.
    -   `DatabaseMiddlewareWithCommit()`: Middleware для управления сессиями базы данных с фиксацией изменений.

**Примеры**:

```python
# Пример вызова функции
register_middlewares()
```

### `register_routers`

```python
def register_routers():
    """
    Регистрирует маршруты для диспетчера.
    """
```

**Назначение**: Регистрирует маршрутизаторы для диспетчера (`dp`). Маршрутизаторы определяют, как обрабатывать различные типы входящих запросов на основе их содержимого.

**Параметры**: Нет.

**Возвращает**: Нет.

**Как работает функция**:

-   Включает три маршрутизатора:
    -   `catalog_router`: Маршрутизатор для обработки запросов, связанных с каталогом товаров.
    -   `user_router`: Маршрутизатор для обработки пользовательских команд и запросов.
    -   `admin_router`: Маршрутизатор для обработки административных команд и запросов.

**Примеры**:

```python
# Пример вызова функции
register_routers()
```

### `create_app`

```python
def create_app():
    """
    Создает и настраивает приложение aiohttp.
    """
```

**Назначение**: Создает и настраивает приложение `aiohttp` для обработки входящих запросов от Telegram через вебхуки.

**Параметры**: Нет.

**Возвращает**:

-   `app`: Объект приложения `aiohttp`.

**Как работает функция**:

1.  Создает экземпляр приложения `aiohttp` (`app = web.Application()`).
2.  Регистрирует обработчики маршрутов:
    -   `/{settings.BOT_TOKEN}`: Обработчик для вебхука Telegram (метод `handle_webhook`).
    -   `/robokassa/result/`: Обработчик для получения результатов оплаты через Robokassa (метод `robokassa_result`).
    -   `/robokassa/fail/`: Обработчик для обработки неудачных платежей через Robokassa (метод `robokassa_fail`).
    -   `/`: Обработчик для главной страницы (метод `home_page`).
3.  Настраивает приложение с использованием диспетчера (`dp`) и бота (`bot`), используя функцию `setup_application`.
4.  Регистрирует функции `on_startup` и `on_shutdown` для обработки событий запуска и остановки приложения.

**Примеры**:

```python
# Пример вызова функции
app = create_app()
```

### `main`

```python
def main():
    """
    Главная функция для запуска приложения.
    """
```

**Назначение**: Главная функция для запуска приложения.

**Параметры**: Нет.

**Возвращает**: Нет.

**Как работает функция**:

1.  Регистрирует middleware и маршрутизаторы, вызывая функции `register_middlewares()` и `register_routers()`.
2.  Создает приложение `aiohttp`, вызывая функцию `create_app()`.
3.  Запускает приложение, используя функцию `web.run_app()`. Указывает хост (`settings.SITE_HOST`) и порт (`settings.SITE_PORT`), на котором будет работать приложение.

**Примеры**:

```python
# Пример вызова функции
main()