"""
ZOOP:
=================
קושי: 5
-----------------
המשחק "ZOOP" הוא משחק מילים פשוט בו המחשב בוחר באופן אקראי מילה מתוך רשימה קבועה, והשחקן צריך לנחש את המילה הזו. המשחק מעניק לשחקן רמז בדמות מספר האותיות במילה.

חוקי המשחק:
1. המחשב בוחר מילה אקראית מרשימה מוגדרת מראש.
2. השחקן מזין ניחוש למילה.
3. המשחק מודיע לשחקן את מספר האותיות במילה המוגרלת.
4. אם השחקן ניחש נכונה, המשחק מסתיים בניצחון.
5. אם השחקן טועה, המשחק חוזר לשלב 2.

-----------------
אלגוריתם:
1. הגדר רשימה של מילים אפשריות.
2. בחר מילה אקראית מהרשימה.
3. הצג למשתמש את מספר האותיות במילה הנבחרת.
4. קבל קלט מהמשתמש - ניחוש מילה.
5. אם הניחוש שווה למילה הנבחרת, הצג הודעת ניצחון וסיים את המשחק.
6. אחרת, חזור לשלב 4.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeWords["<p align='left'>אתחול משתנים:
    <code><b>
    wordList = ['ZOOP', 'BOOP', 'LOOP', 'GOOP']
    targetWord = random(wordList)
    </b></code></p>"]
    InitializeWords --> OutputWordLength["הצג: <b>אורך המילה הוא <code>{len(targetWord)}</code> אותיות</b>"]
    OutputWordLength --> InputGuess["קבל ניחוש מהמשתמש: <code><b>userGuess</b></code>"]
    InputGuess --> CheckGuess{"בדיקה: <code><b>userGuess == targetWord?</b></code>"}
    CheckGuess -- כן --> OutputWin["הצגת הודעה: <b>מזל טוב! ניחשת נכון את המילה!</b>"]
    OutputWin --> End["סוף"]
    CheckGuess -- לא --> InputGuess
```

Legenda:
    Start - התחלת התוכנית.
    InitializeWords - אתחול רשימת המילים האפשריות ובחירת מילה אקראית מתוכן.
    OutputWordLength - הצגת מספר האותיות במילה הנבחרת למשתמש.
    InputGuess - קבלת קלט מהמשתמש - ניחוש מילה.
    CheckGuess - בדיקה האם הניחוש של המשתמש שווה למילה הנבחרת.
    OutputWin - הצגת הודעת ניצחון אם הניחוש נכון.
    End - סוף התוכנית.
"""
```python
import random

# רשימת המילים האפשריות
wordList = ['ZOOP', 'BOOP', 'LOOP', 'GOOP']

# בחירת מילה אקראית מהרשימה
targetWord = random.choice(wordList)

# הדפסת אורך המילה
print(f"אורך המילה הוא {len(targetWord)} אותיות")

# לולאה ראשית של המשחק
while True:
    # קבלת ניחוש מהמשתמש
    userGuess = input("נסה לנחש את המילה: ").upper()

    # בדיקה האם הניחוש נכון
    if userGuess == targetWord:
        print("מזל טוב! ניחשת נכון את המילה!")
        break  # סיום המשחק אם הניחוש נכון
    else:
        print("נסה שוב.")  # בקשה לנסות שוב
```
"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
    - `import random`: ייבוא המודול `random`, המשמש לבחירת מילה אקראית מתוך הרשימה.
2.  **הגדרת רשימת מילים**:
    - `wordList = ['ZOOP', 'BOOP', 'LOOP', 'GOOP']`: הגדרת רשימה של מילים אפשריות.
3.  **בחירת מילה אקראית**:
    - `targetWord = random.choice(wordList)`: בחירת מילה אקראית מתוך הרשימה ושמירתה במשתנה `targetWord`.
4. **הצגת רמז**:
    - `print(f"אורך המילה הוא {len(targetWord)} אותיות")`: הדפסת אורך המילה הנבחרת כרמז למשתמש.
5.  **לולאת המשחק `while True:`**:
    - לולאה אינסופית, הממשיכה עד שהמשתמש מנחש את המילה (הפקודה `break` תסיים את הלולאה).
    - **קבלת ניחוש מהמשתמש**:
        - `userGuess = input("נסה לנחש את המילה: ").upper()`: קבלת ניחוש מהמשתמש והמרתו לאותיות גדולות כדי להשוות בצורה לא רגישה לאותיות קטנות.
    -  **בדיקת ניחוש**:
        - `if userGuess == targetWord:`: בדיקה האם הניחוש של המשתמש שווה למילה שנבחרה.
        - `print("מזל טוב! ניחשת נכון את המילה!")`: אם הניחוש נכון, מודפסת הודעת ניצחון.
        - `break`: סיום הלולאה (והמשחק) אם הניחוש נכון.
        - `else:`: אם הניחוש לא נכון.
        - `print("נסה שוב.")`: בקשה מהמשתמש לנסות שוב.
"""
